# Required metadata
sonar.projectKey=uptime-portal
sonar.projectName=uptime-portal

# Comma-separated paths to directories with sources (required)
sonar.sources=src/app   #Error

# Language
sonar.language=ts

# Encoding of the source files
sonar.sourceEncoding=UTF-8
